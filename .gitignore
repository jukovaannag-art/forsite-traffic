__pycache__/
*.pyc
.venv/
.pytest_cache/
.streamlit/secrets.toml
# Токен GitHub для заливки и для Worker'а. В репозиторий не попадает никогда:
# репозиторий публичный, и утёкший токен даёт запись в него же.
.env
# Служебный кэш wrangler, появляется при каждой выкладке воркера.
.wrangler/
